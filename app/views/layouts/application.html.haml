<!DOCTYPE html>
%html
  %head
    %title
      Tuxedoweasels
    = stylesheet_link_tag    "application", :media => "all" 
    = javascript_include_tag "application" 
    = csrf_meta_tags 
  %body
    #bar
      = render 'layouts/header' 
    - flash.each do |key, value| 
      %div{ :class => "flash #{key}" }= value 
    #main
      = yield 
    
    = debug(params) if Rails.env.devlopment? 
